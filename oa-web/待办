流程的监听器...流程通知监听器，

如果要让人审核，那么申请的方式要变，按照船只博客上的来。

http://blog.csdn.net/zjx86320/article/details/50412263/

ACT_ID_GROUP, ACT_ID_USER和自己的用户组和用户表中的数据是一样的。

ACT_ID_MEMBERSHIP和USER_GROUP也是一样的。

HistoricIdentityLinkEntity，我怀疑是 delegateTask.addCandidateGroups(groupIds);的时候把数据插入了这张表中，看一下数据就知道了。

这个系统中，是按照用户组类型来分配任务的，而在流程监听器中，则把这个类型下的所有组都添加到了候选组中。这个添加动作会持久化到act_hi_identitylink中，而不是把用户ID持久化进去，
需要拿用户ID到ACT_ID_MEMBERSHIP中找到所有的组ID，再到act_hi_identitylink中查询。

----------------------------------------------------------
2016年5月2日
关键就是单元测试。
js插件的写法
实体类的设计

我没必要把菜单和角色加进去，因为我这个项目不重视这一点。

按照需求来写。。而不是把所有的技术都塞进来。

7.actitivi的单元测试要跑一遍。
1.把消息加上。按照lemon来
2.写一个jquery插件。
3.加上流程向导，放到详情页面
4.流程的单元测试

5.加一个富文本填写，和预览来熟悉一下js和css
6.把发送邮件加上

//做一个附件管理。

//消息订阅--改为文章的订阅，你订阅一部分文章的标签，然后一篇文章发布的时候，如果标签对应你订阅的则发送给你消息

//jplayer

//百度文库，文章在线预览 --最代码

//二维码

//webservice

//lunch
//Quartz

//角色和学院的关系，用户和学院的关系。用户和角色的关系

//-------------------------------------
一切已经就绪


